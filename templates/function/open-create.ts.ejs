const openCreate<%= tableNameRelatedCase.pascal %> = () => {
    const id = openDialog({
      title: t('create', { ns: 'actions' }),
      description: t('createText', { ns: '<%= tableNameRelatedCase.kebab %>' }),
      children: () => (
        <<%= tableNameRelatedCase.pascal %>CreatePanel onCreated={() => closeDialog(id)} />
      ),
    })

    return id
}