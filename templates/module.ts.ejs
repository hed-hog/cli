import { AdminModule } from '@hedhog/admin';
import { PaginationModule } from '@hedhog/pagination';
import { PrismaModule } from '@hedhog/prisma';
import { forwardRef, Module } from '@nestjs/common';
<% if (options.importServices) { %>
import { <%= tableNameCase.pascal %>Service } from './<%= tableNameCase.kebab %>.service';
import { <%= tableNameCase.pascal %>Controller } from './<%= tableNameCase.kebab %>.controller';`
<% } %>

@Module({
    imports: [
        forwardRef(() => AdminModule),
        forwardRef(() => PrismaModule),
        forwardRef(() => PaginationModule),
    ],
    controllers: <%= options.importServices ? `[<%= tableNameCase.pascal %>Controller]` : '[]' %>,
    providers: <%= options.importServices ? `[<%= tableNameCase.pascal %>Service]` : '[]' %>,
    exports: <%- options.importServices ? `[forwardRef(() => <%= tableNameCase.pascal %>Service)]` : '[]' %>,
})
export class <%= tableNameCase.pascal %>Module {}